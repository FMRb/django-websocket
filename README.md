django-websocket
================

Example of django and websocket using gvent-socketio and socketio.js

$ cd django-websocket
$ python bootstrap.py
$ ./bin/buildout
$ ./bin/django syncdb
$ ./bin/django runserver_socketio
